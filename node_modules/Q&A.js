/**
 * Created by WangZheZen on 2/3/2017.
 */
var request = require('request');

var options = {
    url: 'https://westus.api.cognitive.microsoft.com/luis/v2.0/apps/ae60bd7a-97ad-4dd3-925d-44b2db7fcd17?subscription-key=7a78a990591f446e8c5943fe0cd73a76&q=dc%202nd%20floor&timezoneOffset=0.0&verbose=true',
    headers: {
     //   'User-Agent': 'request'
    }
};

function callback(error, response, body) {
    if (!error && response.statusCode == 200) {
        var info = JSON.parse(body);
       return  console.log(info.entities);
//.intents[0].actions[0].parameters[0].value[0].entity
    }
}

request(options, callback);